# Tests Audit History

This file is the unity-tests-audit skill's persistent state. It contains:

- **Configuration**: project-specific overrides (mechanic patterns, thresholds)
- **User Corrections**: per-member overrides that take precedence over default heuristics
- **Audit Runs**: append-only history of past audits

The skill auto-appends to "Audit Runs" on each run. The user manually edits "Configuration" and "User Corrections" sections.

---

## Configuration

(Optional — defaults are used if missing)

### Mechanic Patterns (Type A only)

(N/A — `com.gamelovers.gamedata` is a Type C standalone UPM package; no gameplay mechanics.)

### Thresholds

- thoroughModeThreshold: 10  (Quick mode under this; Thorough at or above)
- divergenceThreshold: 0.30  (Layer 4 adjacent disagreement re-run threshold)

---

## User Corrections

The Judge consults this section before applying default heuristics from `categories.md`. User-provided overrides take precedence.

| Type.Member | Override | Signal | Reason | Added |
|---|---|---|---|---|

(no corrections yet)

---

## Audit Runs

(Append-only. The skill writes a new entry below on each audit completion.)

## Audit Run Follow-up — 2026-04-27T10:48:00+03:00

User-requested follow-up to the 2026-04-27T09:52:00+03:00 baseline audit. No new Finder/Judge/Referee dispatch — applies the previously-recorded recommendations.

### Changes applied

**Skill repo (`~/.claude/skills/unity-tests-audit/scripts/run-tests.sh`)**:
- Replaced `pgrep -f "Unity.*$(basename PROJECT_PATH)"` lock detection (false-positive against Unity Hub Electron processes whose paths contain `Unity Hub.app/Contents/Frameworks/...`) with a binary-specific check that matches only the actual Unity Editor binary plus the project path: `pgrep -fl "Unity\.app/Contents/MacOS/Unity"` (macOS) or `Unity.*Editor/.*/Editor/Unity` (Linux), AND `-projectPath <PROJECT_PATH>`.

**Runtime sources (this package)**:
- `Runtime/AssemblyInfo.cs` (NEW): `[assembly: InternalsVisibleTo("GameLovers.GameData.Editor.Tests")]` to expose internal seams to the test assembly. Mirrors the existing `Editor/AssemblyInfo.cs` pattern.
- `Runtime/Utilities/EnumSelector.cs`: added `internal void SetSelectionString(string)` test seam.
- `Runtime/Serialization/SerializableType.cs`: added `internal static SerializableType<T> FromSerializedNames(string, string)` test seam; extracted `OnAfterDeserialize` body into `private void OnAfterDeserializeImpl()` to avoid struct-boxing-via-interface-cast loss.
- `Runtime/Serialization/UnitySerializedDictionary.cs`: added `internal List<TKey> KeyDataInternal`, `internal List<TValue> ValueDataInternal`, `internal void SetSerializedLists(List<TKey>, List<TValue>)` test seams.

**Tests (this package)** — **white-box anti-patterns eliminated**:
- `Tests/Editor/Unit/EnumSelectorTest.cs`: 3 tests (`GetSelection_InvalidSelection_ReturnsFirstValue`, `HasValidSelection_RemovedEnumValue_ReturnsFalse`, `HasValidSelection_EmptyString_ReturnsFalse`) refactored to use `SetSelectionString` instead of `BindingFlags.NonPublic` reflection on `_selection`.
- `Tests/Editor/Unit/SerializableTypeTest.cs`: `Value_Property_ResolvesCorrectly` refactored to use `FromSerializedNames` factory instead of reflection on `_className`/`_assemblyName` + struct-boxing dance.
- `Tests/Editor/Unit/UnitySerializedDictionaryTest.cs`: 2 tests (`OnAfterDeserialize_OverwritesDuplicateKeys`, `OnBeforeSerialize_PopulatesLists`) refactored to use `SetSerializedLists` / `KeyDataInternal` / `ValueDataInternal` instead of reflection on `_keyData`/`_valueData`.

Net result: **0 reflection-on-private hits** remain in `Tests/` (was 8 hits across 3 files per the baseline Referee report).

**Tests (this package)** — **12 Medium-confidence stubs added** (16 individual `[Test]` methods):
- `Tests/Editor/Unit/MigrationRunnerTest.cs` (extended): `MigrateScriptableObject_AppliesMigrations_UpdatesObject`, `MigrateScriptableObject_FromAtOrAboveTo_ReturnsNoMigrations` + `MockScriptableConfig` fixture + `[ConfigMigration]` registration.
- `Tests/Editor/Unit/floatPTests.cs` (extended): `FromParts_RoundTrip_ReconstructsRawValue`, `IeeeRaw_RoundTrip_PreservesValue`, `Sign_NaNArgument_Throws`.
- `Tests/Editor/Unit/MathfloatPTest.cs` (extended): `DivRem_KnownPair_ReturnsExpectedRemainderAndQuotient`, `IEEERemainder_KnownPair_MatchesDivRemRemainder`, `Mod_KnownPair_ReturnsExpectedRemainder`, `LogTwoArg_BaseAndDomain_HandlesSpecialCases`, `RawDistance_FiniteAndInfinite_ReturnsExpected`.
- `Tests/Editor/Unit/SortedListExtensionsTest.cs` (NEW): 4 tests covering generic `IList<T>` overloads (`Comparable<T>`, `Comparison<T>`, `IComparer<T>` paths) with non-decreasing / non-increasing / duplicate scenarios.
- `Tests/Editor/Unit/ReflectionExtensionsTest.cs` (NEW): 2 tests covering both `Activator.CreateInstance` happy path and the non-public-ctor fallback branch.

### Test execution after follow-up

| Platform | Total | Passed | Failed | Skipped | Inconclusive | Duration |
|---|---|---|---|---|---|---|
| EditMode | **615** | 615 | 0 | 0 | 0 | ~27s |
| PlayMode | **205** | 205 | 0 | 0 | 0 | ~19s |
| **Combined** | **820** | **820** | **0** | **0** | **0** | ~46s |

Delta vs. baseline audit run: +16 tests in EditMode (12 stub bullets → 16 `[Test]` methods because `SortedListExtensionsTest` and `MigrateScriptableObject_*` were structured as multiple discrete tests). All 6 white-box-refactored tests still pass with the new seams (verified individually).

### Final cleanup pass — 2026-04-28T00:11:00+03:00

After the follow-up changes were verified green, applied a small cleanup:

- **Deleted** `Tests/Editor/Regression/RegressionTests.cs` + `.cs.meta` (placeholder file with zero active `[Test]` methods, only a commented-out template). Per `categories.md` §2.7 regression tests come from real bugs and shouldn't exist speculatively.
- **Deleted** the now-empty `Tests/Editor/Regression/` folder + its `Regression.meta`. Unity will regenerate both when a real regression test is added.
- **Stripped** explanatory comment blocks from the new internal seams (`SetSelectionString`, `OnAfterDeserializeImpl`, `FromSerializedNames`, `KeyDataInternal` / `ValueDataInternal` / `SetSerializedLists`). Per project convention, only public non-MonoBehaviour methods or methods carrying critical info get summary docs; internal test seams should stay terse.

#### Re-verification after cleanup

| Platform | Gamedata-only | Total (all assemblies) | Notes |
|---|---|---|---|
| EditMode | **356/356 PASSED** | 621/623 (2 unrelated services-package failures) | Failures: `GameLoversEditor.Services.Tests.RngServiceTest.Range_IntMaxInclusiveTrue_CanReturnMaxBoundary` (statistical/flaky boundary draw) and `VersionServicesTest.IsOutdatedVersion_DirectInvocation_AgreesWithLocalVersion` (`IndexOutOfRangeException` at `VersionServices.cs:106`). Both pre-exist in `com.gamelovers.services` (separate submodule, not touched by this audit). |
| PlayMode | **5/5 PASSED** (gamedata.PlayMode.Tests.dll) | 208/208 | All assemblies green. |

Gamedata package: **361 / 361 tests passing**. All 14 new stubs from the follow-up + 6 white-box-refactored tests still pass after the deletions and comment cleanup.

### Coverage Delta (cumulative since baseline)

| Metric | Baseline (Run 1) | After Follow-up | Delta |
|---|---|---|---|
| Total tests in suite | 804 | 820 | +16 |
| P0 untested | 1 | 0 | -1 (`MigrateScriptableObject<T>` covered by 2 stubs) |
| P2 untested | ~22 | ~14 | -8 (math + extensions stubs) |
| White-box reflection-on-private hits in Tests/ | 8 (across 3 files) | **0** | -8 (replaced by `[InternalsVisibleTo]` + 4 internal seam APIs) |
| Stub recommendations approved & written | 1 (High) | 13 (1 High + 12 Medium) | +12 |

### Outstanding items (not addressed this run)

- 4 Low-confidence stubs (`Combine`, `RegisterAllowedTypesFromProvider`, `BindToName`, `IsPositive_IsNegative`) — Judge's sticky cases; deferred per filter setting. Add user corrections to `## User Corrections` if you'd like them auto-skipped on future runs.
- Empty `Tests/Editor/Regression/RegressionTests.cs` placeholder — surfaced for user decision (delete or populate per `categories.md` §2.7).
- Orphaned classifications: `UnityObjectsExtensions.GetLocalCornersArray` and `GetWorldCornersArray` (PlayMode + RectTransform required) — recommend skip-recommending in future audits.

---

## Audit Run — 2026-04-27T09:52:00+03:00

**Scope**: `Packages/com.gamelovers.gamedata/`
**Project type**: `C` (standalone UPM package — host repo is Type B but the audit scope is one self-contained package)
**Mode**: `Thorough` (Finder + Judge + Referee; all 8 anti-gaming layers active; user-requested via "in thorough mode")
**Trigger mode**: `Standalone` (no feature-start or wrap-up context)
**Duration**: Phase 1 ≈ 11m; Phase 2 ≈ 1m (compile + EditMode + PlayMode + 1 stub iteration)

### Coverage Delta

| Metric | Before | After | Delta |
|---|---|---|---|
| Total public API members | 0 (first audit) | ~210 | (baseline) |
| Tested members | 0 (baseline) | ~181 (raw ~180 + 1 written stub) | +1 |
| Coverage % (rough) | n/a | ~86% | (baseline) |
| P0 untested | n/a | 1 (down from 2 — `AddAllConfigs` closed; `MigrateScriptableObject<T>` remains, Medium-confidence stubs not approved this run) | -1 |
| P1 untested | n/a | 2 (both Skip-recommended due to Red testability) | 0 |
| Total tests in suite | n/a | **804** (599 EditMode + 205 PlayMode) | (baseline) |
| Failing tests | n/a | **0** | (green baseline established) |

This is the first audit; "Before" values are baselines.

### Anti-Cheat Layer Pass Rate

| Layer | Status |
|---|---|
| L1 — Verifiable Output | PASS (Finder: 5/5 spot-checks exact, 8/8 citations exist; Judge: 15/15 citations exist, 17/17 test names non-conflicting; Referee: spotted one off-by-25 line citation in Judge's output) |
| L2 — Asymmetric Information | PASS (Judge produced Conclusions + Reasoning; Reasoning stripped before forwarding to Referee) |
| L3 — Adversarial Framing | PASS (Referee Verification Log produced; Referee found genuine errors — see "Notable Findings") |
| L4 — Independent Sample Re-Derivation | PASS (5 random samples re-derived; 0/5 critical, 0–1/5 adjacent disagreements; well under 30% re-run threshold) |
| L5 — Output Caps | PASS (16/20 stubs, ≤5 per file, 10 skips at cap, E2E sub-types correctly Skipped for Type C) |
| L6 — Different Models Per Role | DEGRADED (single-model fallback — Cursor inherits same model across roles; not a failure but Layer 6 mitigation is reduced) |
| L7 — Pre-Flight Checklist | PASS (3 distinct priorities present, two-section Judge output, no out-of-scope findings) |
| L8 — Anchor | PASS (Finder anchor: `11==11`; Judge anchor: `P0, P0:ObservableSetter` exact match; Referee strengthened anchor: 4/5 exact, 1/5 adjacent — adjacent caught a main-agent error in expected `Deserialize<T>` priority, not a Referee error) |

### Notable Findings

- **2 P0 untested gaps closed** by recommendation: `ConfigsProvider.AddAllConfigs` (1 stub written) and `MigrationRunner.MigrateScriptableObject<T>` (2 stubs proposed but Medium confidence — not approved this run).
- **Pyramid shape**: Steep Pyramid ✓ (Unit ≈ 89% of ~330 tests; E2E = 0). Within Type C tolerance.
- **Design smells (Red testability) — 4 from Judge + 3 from Referee**:
  - Judge: `IConfigBackendService` interface-only contract; `ConfigsScriptableObjectInspector.CreateInspectorGUI`; `ObservableDebugRegistry.EnumerateSnapshots/FindByInstance`; `UnityObjectsExtensions.RaycastPoint`.
  - Referee correction: 3 test files use `BindingFlags.NonPublic | Instance` reflection on private fields (`EnumSelectorTest.cs:66/80/93`, `SerializableTypeTest.cs:24-25`, `UnitySerializedDictionaryTest.cs:71-72/92-93`). The Judge claimed "no white-box smells" — Referee corrected this. Recommended fixes: add `[InternalsVisibleTo("...Tests")]` + internal seams, OR drive scenarios via real `ScriptableObject` round-trips.
- **Empty `RegressionTests.cs`** flagged: file has no active `[Test]`, only a placeholder inside `/* … */`. Either populate with a real bug-driven regression test or remove.
- **Judge cited `ConfigsProviderDebugRegistry.EnumerateSnapshots` at line 95**; correct line is **70** (off-by-25).
- **Orphaned classifications** (Judge): `UnityObjectsExtensions.GetLocalCornersArray` and `GetWorldCornersArray` were classified P2/Yellow but appear in neither stubs nor skips. Recommend skip-recommending both with rationale "PlayMode + GameObject required; thin wrappers".
- **Skip list cap accounting**: 10 bullets / 11 members (one bullet lumps two members). Borderline at Layer 5 cap.

### User Corrections Applied This Run

- Total corrections in `.audit-history.md`: 0 (first audit)
- Corrections that affected this audit's findings: 0

### Stubs Approved by User

- 1 stub approved and written: `ConfigsProviderTest.AddAllConfigs_BulkPayload_RegistersAllTypes` in `Packages/com.gamelovers.gamedata/Tests/Editor/Unit/ConfigsProviderTest.cs` (extends existing fixture).
- User filter: High confidence only (1/16 stubs).
- 15 stubs (11 Medium + 4 Low confidence) NOT written this run; documented in this audit run for follow-up.

### Phase 2 Status (Test Execution)

- Editor state: CLOSED at run-time (only Unity Hub running; user clarified the lockfile + `pgrep -f "Unity.*Frameworks"` heuristic was a false positive against Unity Hub's Electron `Frameworks/` path component)
- Unity MCP availability at run-time: NOT LOADED (`user-unity-mcp` descriptors exist on disk but the MCP server was not connected to the audit session)
- **Execution method**: CLI batch mode via `Unity 6000.3.7f1` (bypassed `scripts/run-tests.sh` because its `pgrep -f "Unity.*$(basename PROJECT_PATH)"` lock check matched Unity Hub Electron processes; ran `Unity -runTests -batchmode -nographics` directly)

#### EditMode

| Status | Count |
|---|---|
| Passed | 599 |
| Failed | 0 |
| Skipped | 0 |
| Inconclusive | 0 |
| **Total** | **599** |

Duration: ~40s (compile + run). Result XML: `/tmp/unity-tests-audit/com.gamelovers.gamedata/EditMode-results.xml`. Log: `EditMode-log.txt`.

#### PlayMode

| Status | Count |
|---|---|
| Passed | 205 |
| Failed | 0 |
| Skipped | 0 |
| Inconclusive | 0 |
| **Total** | **205** |

Duration: ~18s. Result XML: `/tmp/unity-tests-audit/com.gamelovers.gamedata/PlayMode-results.xml`. Log: `PlayMode-log.txt`.

#### New stub result

- `GameLovers.GameData.Tests.ConfigsProviderTest.AddAllConfigs_BulkPayload_RegistersAllTypes` → **PASSED** (1.9 ms; `EditMode-results.xml` `<test-case id="1054" result="Passed">`).

#### Stub iteration note

First run of the new stub failed compilation: `IEnumerable` resolved to `System.Collections.Generic.IEnumerable<T>` (because the test file imports `System.Collections.Generic` but not `System.Collections`), conflicting with `AddAllConfigs(IReadOnlyDictionary<Type, IEnumerable>)`'s non-generic `System.Collections.IEnumerable`. Fixed by using the fully-qualified name `System.Collections.IEnumerable` (matching the existing `UpdateTo_*` test in the same file). Lesson for future audits scoped to this fixture: always fully-qualify `System.Collections.IEnumerable` when invoking `AddAllConfigs`/`UpdateTo` in `ConfigsProviderTest.cs`.

### Static Analysis Artifacts

- Finder report: `/tmp/unity-tests-audit/com.gamelovers.gamedata/finder-output.md`
- Judge Conclusions (Reasoning stripped, forwarded to Referee): `/tmp/unity-tests-audit/com.gamelovers.gamedata/judge-conclusions.md`

### Recommendations for Next Audit

- Add user corrections (above) for any classifications you disagree with — the `Combine`, `RegisterAllowedTypesFromProvider`, and `BindToName` Low-confidence stubs are good candidates if you decide to Skip them.
- Consider promoting the 3 Referee-detected white-box test smells to a refactor PR: add `[InternalsVisibleTo("GameLovers.GameData.Tests.Editor")]` to the runtime assembly + minimal `internal` seams (`SetSelectionString`, `FromTypeName`, `GetSerializedLists`/`SetSerializedLists`).
- Address the empty `RegressionTests.cs` (delete or populate per `categories.md` §2.7).

---
